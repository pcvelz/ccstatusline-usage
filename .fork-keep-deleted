# Files that exist in upstream but are intentionally removed in this fork.
# Listed paths are automatically re-deleted during `git merge upstream/main`.
# Add new entries here when upstream re-introduces a file we've decided to drop.

docs/DEVELOPMENT.md
docs/USAGE.md
docs/WINDOWS.md

# Upstream's tag-triggered npm-publish-with-provenance workflow. The fork
# publishes manually via the /release skill, so this would double-publish and
# create conflicting GitHub releases. Hardcodes upstream repo/package names too.
.github/workflows/publish.yml
